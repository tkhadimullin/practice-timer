version: '3.8'

services:
  practice-timer:
    build: .
    ports:
      - "3000:80"
    restart: unless-stopped
    container_name: practice-timer-app
    
  # Development service (uncomment for development)
  # dev:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.dev
  #   ports:
  #     - "5173:5173"
  #   volumes:
  #     - .:/app
  #     - /app/node_modules
  #   environment:
  #     - NODE_ENV=development
  #   command: npm run dev